import styled from 'styled-components';

export const StyledSubmitButton = styled.button`
    padding: 15px 0;

    font-size: 16px;
    line-height: 19px;
    color: #ffffff;

    background: #23c9ff;
    border-radius: 10px;
`;
