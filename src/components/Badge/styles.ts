import styled from 'styled-components';

export const StyledBadge = styled.span`
    display: block;
    padding: 3px 13px;

    color: #fff;
    font-size: 12px;
    line-height: 15px;

    background: #23c9ff;
    border-radius: 40px;
`;
